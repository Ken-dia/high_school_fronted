import { FilterByClassePipe } from './filter-by-classe.pipe';

describe('FilterByClassePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByClassePipe();
    expect(pipe).toBeTruthy();
  });
});
